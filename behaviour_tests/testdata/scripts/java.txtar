# dh java --json outputs valid JSON with expected keys
exec dh java --json
stdout '"found":'
stdout '"version":'
stdout '"path":'
stdout '"source":'

# dh java shows human-readable status
exec dh java
stdout 'Java:'

# dh java install --help shows expected flags
exec dh java install --help
stdout '\-\-jdk-version'
stdout '\-\-force'
