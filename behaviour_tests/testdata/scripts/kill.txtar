# dh kill with non-existent port exits non-zero with error message
! exec dh kill 99999
stderr 'no server found'

# dh kill --json with non-existent port outputs JSON error
! exec dh kill --json 99999
stderr 'server_not_found'
