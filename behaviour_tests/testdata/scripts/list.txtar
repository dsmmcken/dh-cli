# dh list outputs human-readable text (may be empty if no servers running)
exec dh list
! stderr .

# dh list --json outputs valid JSON with "servers" key
exec dh list --json
stdout '"servers"'
! stderr .

# dh list -j is shorthand for --json
exec dh list -j
stdout '"servers"'
